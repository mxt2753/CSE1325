#include "Vending_machine.h"
#include <iostream>

int main() {
    Vending_machine vm;

    vm.add("Oreos", 189);
    vm.add("Snickers", 150);

    std::cout << vm.menu() << std::endl;

    vm.buy(1);

    return 0;
}
